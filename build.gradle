apply plugin: 'java'

sourceSets.main.java.srcDir 'src'

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'lib')
    compile project(':measure')
}

sourceCompatibility = JavaVersion.VERSION_1_7
targetCompatibility = JavaVersion.VERSION_1_7
